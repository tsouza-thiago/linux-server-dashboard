# Copie este arquivo para .env e preencha com SEUS valores reais.
# NUNCA commite o .env (ele está no .gitignore). Não armazene senhas aqui:
# este projeto autentica por chave SSH.
SSH_HOST=seu_host_ou_alias_ssh
POLL_INTERVAL=60000
PORT=3000
HISTORY_LIMIT=4320
HISTORY_FILE=data/history.json
LOG_FILE=data/dashboard.log
# Interface de rede a monitorar (ex.: enpXsY). Vazio = seção Rede omitida.
NET_IF=
# Mount points monitorados, separados por espaço (ex.: / /mnt/disco1).
DISK_MOUNTS=/
# Dispositivos de bloco para IO/SMART, separados por espaço (ex.: sda sdb). Vazio = omitido.
DISK_DEVS=
# Serviços systemd monitorados, separados por espaço (ex.: smbd nmbd). Vazio = omitido.
SERVICES=
# Token de acesso à API/SSE. Se preenchido, exige Authorization: Bearer <token>
# (o navegador pedirá o token uma vez). O install.sh gera um automático se vazio.
# Gere um manualmente com:  openssl rand -hex 16
DASH_TOKEN=